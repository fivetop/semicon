# 작업 지시 이력 정리 (2026-03-08)

## 1) 작업 시작 및 폴더 이동
- 어제 하던 작업 이어서 진행 요청.
- 작업 기준 폴더를 `DC_2016.03`로 전환.
- 이후 작업 폴더를 `romee`로 전환.

## 2) Lab 분석 및 문서화

### 2-1. lab3
- `romee/lab3`의 설정 파일(`.synopsys_dc.setup`, `common_setup.tcl`, `dc_setup.tcl`), 실행 스크립트(`.solutions/dc.tcl`), 제약(`.solutions/MY_DESIGN.con`), RTL(`rtl/MY_DESIGN.v`) 분석 수행.
- 핵심: 기본 DC 합성 흐름(라이브러리 설정 -> RTL 링크 -> 제약 적용 -> 타이밍 리포트 -> DDC 저장).
- 저장: `romee/doc/lab3_analysis.md` 업데이트.

### 2-2. lab4
- `romee/lab4` 분석 수행.
- 핵심: lab3 대비 환경 제약(입력 구동셀, 입력 transition, 출력 load) 추가.
- 저장: `romee/doc/lab4_analysis.md` 업데이트.

### 2-3. lab5
- `romee/lab5` 분석 수행 (`STOTO` 설계).
- 핵심: 고급 최적화(`compile_ultra`), path group, retiming 제어, incremental compile, physical constraints(`STOTO.pcon`).
- 저장: `romee/doc/lab5_analysis.md` 신규 생성.

### 2-4. lab7
- `romee/lab7` 분석 수행.
- 핵심: 네거티브 엣지 타이밍 모델링 2가지 비교
  1. 단일 클럭 + latency included 방식
  2. virtual clock(`F4_clk`, `F5_clk`) 분리 방식
- 저장: `romee/doc/lab7_analysis.md` 신규 생성.

### 2-5. lab8
- `romee/lab8` 분석 수행.
- 핵심: RTL 합성 중심이 아니라, `EXCEPTIONS.ddc` 기반 타이밍 예외 제약(False Path / Clock Group / Multicycle) 실습.
- 저장: `romee/doc/lab8_analysis.md` 신규 생성.

## 3) Lab 비교 정리
- `lab1`부터 `lab8`까지 비교 요청에 대해, `romee` 기준 실존 폴더(`lab1,3,4,5,7,8`) 중심 비교 제공.
- 주의: `romee`에는 `lab2`, `lab6` 폴더 없음.
- 저장: `romee/doc/romee_summary.md`를 최신 비교표/학습 흐름으로 업데이트.

## 4) 압축 작업
- 요청에 따라 `romee` 폴더를 zip으로 압축.
- 결과 파일: `/DATA/home/edu118/romee.zip`
- 확인된 크기: 약 `85M`

## 5) 현재 문서 상태 요약
- `romee/doc/lab1_analysis.md`
- `romee/doc/lab3_analysis.md`
- `romee/doc/lab4_analysis.md`
- `romee/doc/lab5_analysis.md`
- `romee/doc/lab7_analysis.md`
- `romee/doc/lab8_analysis.md`
- `romee/doc/romee_summary.md`
- `romee/doc/work_log_2026-03-08.md` (본 문서)
